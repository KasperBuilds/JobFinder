{
  "name": "ahs-job-portal",
  "version": "1.0.0",
  "description": "Job aggregator portal for international relations, political science, law, and related fields",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start:prod": "cd server && npm start"
  },
  "keywords": ["job-portal", "international-relations", "political-science", "law"],
  "author": "AHS Job Portal",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
